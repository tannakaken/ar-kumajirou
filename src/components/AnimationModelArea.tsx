import { TCanvas } from "./TCanvas";

export const AnimationModelArea = () => {
  return (
    <div
      style={{
        position: "relative",
        width: "100%",
        height: "100%",
      }}
    >
      <TCanvas />
    </div>
  );
};
